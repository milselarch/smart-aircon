// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.potatocon.testing"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
